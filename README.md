# dummy-poc

Minimal disposable test app used to exercise the self-service deployment
pipeline (tag → build → push to Artifact Registry → deploy to Cloud Run)
end to end, before wiring it up to any real POC.

Not a real POC — safe to tag, rebuild, and redeploy as many times as needed
while testing.
